spring:
  application:
    name: kotlin-springboot-REST-API-practice
  datasource:
    # Usa o banco padrão 'postgres' (já existe). Para um banco dedicado: crie com CREATE DATABASE rest_with_spring_boot e altere aqui.
    url: jdbc:postgresql://localhost:5432/postgres?useTimezone=true&serverTimezone=UTC
    username: postgres
    password: Maya19082006!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
